import React from 'react';
import Particles from 'react-particles-js';

const particleParams = {
  particles: {
    number: {
      value: 100
    },
    shape: {
      type: 'triangle',
    }
  }
}

const Home = (props) => {
  return (
    <div>
    </div>
  );
}

export default Home;
